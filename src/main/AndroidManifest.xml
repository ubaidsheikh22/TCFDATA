<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tcfapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Setting"></activity>
        <activity android:name=".ChatBox" />
        <activity android:name=".FillForm" />
        <activity android:name=".Sitevisite" />
        <activity android:name=".MainProjProgress" />
        <activity android:name=".ConsProjProgress" />
        <activity android:name=".ProjectList" />
        <activity android:name=".CreateProject" />
        <activity android:name=".SelectRegionActivity" />
        <activity android:name=".ConstructionPanelActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>